<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<!-- 맵핑될 DAO 인터페이스의 Full name 을 줍니다. --> 
<mapper namespace="ProductSQL"> 
	
	<select id="selectProductList" resultType="com.project.shop.model.Product"> 
		SELECT 
			  PRDT_CODE,
			  PRDT_KOR_NAME,
			  PRDT_ENG_NAME,
			  PRDT_SELL_PRICE,
			  PRDT_BRAND_ID,
			  PRDT_DISP_YN,
			  PRDT_REG_ID,
			  PRDT_REG_DT,
			  PRDT_MOD_ID,
			  PRDT_MOD_DT
		FROM 
		 	SHOP_PRODUCT
	</select> 
	
</mapper>