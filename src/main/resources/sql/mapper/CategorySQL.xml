<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="CategorySQL"> 
	<select id="selectCategoryList" resultType="com.project.shop.model.Category">
		SELECT	CTGR_SEQ
				, CTGR_ID
				, PRNT_CTGR_ID
				, CTGR_NAME
				, USE_YN
				, DISP_ORDER
				, CTGR_IMG_URL
				, REG_ID
				, REG_DT
				, MOD_ID
				, MOD_DT
		FROM	SHOP_CATEGORY
		START WITH CTGR_ID IN (SELECT CTGR_ID FROM SHOP_CATEGORY WHERE PRNT_CTGR_ID IS NULL)
		CONNECT BY PRIOR CTGR_ID = PRNT_CTGR_ID
	</select> 
</mapper>